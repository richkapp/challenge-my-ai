name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.22.3
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11
      - run: bun install --frozen-lockfile
      - name: Typecheck
        run: env -u DATABASE_URL NODE_ENV=test CMAI_RUNTIME_ENV=test bun run typecheck
      - name: Test
        run: env -u DATABASE_URL NODE_ENV=test CMAI_RUNTIME_ENV=test bun run test
      - name: Build
        run: env -u DATABASE_URL NODE_ENV=test CMAI_RUNTIME_ENV=test bun run build
